import WordMark from "@/components/WordMark";

// Se ha separado el componente NavBar del componente Header ya que este es de tipo cliente y el otro es de tipo servidor
export default function NavBar() {
  return (
    <div>
        <WordMark />
    </div>
  )
}
